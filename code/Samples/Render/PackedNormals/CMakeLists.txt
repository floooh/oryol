oryol_begin_app(PackedNormals windowed)
    oryol_sources(.)
    oryol_deps(Render)
    oryol_add_web_sample(PackedNormals "Shapes with packed vertex normals" "emscripten,android,pnacl" PackedNormals.jpg "Render/PackedNormals/PackedNormals.cc")    
oryol_end_app()
