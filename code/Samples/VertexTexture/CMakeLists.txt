fips_begin_app(VertexTexture windowed)
    fips_vs_warning_level(3)
    fips_files(VertexTexture.cc)
    fips_generate(TYPE Shader FROM shaders.shd)
    fips_deps(Gfx Assets Time Dbg)
    oryol_add_web_sample(VertexTexture "Vertex texture fetch from offscreen render target" "emscripten,android,pnacl" VertexTexture.jpg "VertexTexture/VertexTexture.cc")
fips_end_app()
