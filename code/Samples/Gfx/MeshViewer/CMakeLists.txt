fips_begin_app(MeshViewer windowed)
    fips_files(MeshViewer.cc)
    fips_generate(TYPE Shader FROM shaders.shd)
    fips_deps(Assets HTTP Input IMUI)
    oryol_add_web_sample(MeshViewer "Load and render .omsh mesh files" "emscripten,pnacl" MeshViewer.jpg "Gfx/MeshViewer/MeshViewer.cc")
fips_end_app()
