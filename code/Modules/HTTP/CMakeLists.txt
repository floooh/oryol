#-------------------------------------------------------------------------------
#   oryol HTTP module
#-------------------------------------------------------------------------------
fips_begin_module(HTTP)
fips_sources(. base)
fips_sources_osx(osx)
fips_sources_linux(curl)
fips_sources_android(curl)
fips_sources_windows(windows)
fips_sources_emscripten(emsc)
fips_sources_pnacl(pnacl)
fips_deps(IO Messaging Core)
if (FIPS_WINDOWS)
    fips_deps(WinHttp)
elseif (FIPS_ANDROID)
    fips_deps(curl zlib)
elseif (FIPS_LINUX)
    fips_deps(curl zlib)
    fips_deps(rt)
endif()
fips_end_module()

fips_begin_unittest(HTTP)
fips_sources(UnitTests)
fips_deps(IO Messaging HTTP Core)
fips_frameworks_osx(Foundation)
fips_end_unittest()
